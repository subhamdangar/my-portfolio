# This file tells npm to ignore platform-specific packages
# that don't match the current operating system.
# This fixes the EBADPLATFORM error when deploying to Vercel (Linux)
# from a Mac development machine.
os=linux
cpu=x64
